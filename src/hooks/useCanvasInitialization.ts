import { useEffect, useState } from 'react';
// STUB: Fabric.js removed for migration
export const useCanvasInitialization = (canvasRef: React.RefObject<HTMLCanvasElement>) => {
  // Always return null
  return null;
};